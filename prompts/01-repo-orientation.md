You are running inside OpenHands Agent Canvas.

Goal: use GitNexus, when available, to study an unfamiliar codebase before
doing broad manual file search.

Task:

1. Study the OpenHands Agent Canvas repo and, if available, the
   OpenHands/software-agent-sdk repo.
2. Use GitNexus MCP tools if they are configured. Start by discovering indexed
   repos, then use graph/search/context tools for the architecture summary.
3. If GitNexus MCP is unavailable, say that briefly and inspect the files
   directly.

Deliver:

- A concise architecture map of Agent Canvas.
- The request path for starting a new conversation from UI to backend.
- The main files or modules involved.
- Three questions a new contributor should ask before changing this area.
- One sentence explaining how GitNexus changed where you started looking.

Keep the answer concrete. Prefer file paths and named components over broad
generalities.
